# Database Configuration
DB_USER=reading_notes
DB_PASSWORD=changeme
DB_NAME=reading_notes

# Application Configuration
APP_PORT=8080
RUST_LOG=info

# Docker Image (will be updated during release)
DOCKER_IMAGE=your-dockerhub-username/reading-notes

# External Database (optional)
# If you want to use an external database instead of the included PostgreSQL container,
# uncomment and configure the following:
# EXTERNAL_DATABASE_URL=postgres://user:password@host:port/database